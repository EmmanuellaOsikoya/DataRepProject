import React from 'react';

//content that will be displayed from the main page
const Substance = () => {
  return (
    <div className="text-center" style={{backgroundImage: 'url(/fruitsalad.jpg)', height: '100vh', backgroundSize: 'cover', backgroundPosition: 'center' }}>
      <h1>Welcome to WhiskAway where tasty recipes are only a whisk away!</h1>
    </div>
  );
}

export default Substance;